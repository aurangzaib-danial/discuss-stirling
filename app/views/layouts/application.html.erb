<!DOCTYPE html>
<html lang="en">
  <head>

    <title><%= yield :title %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <!-- Wrapper is exclusively for implementing sticky footer -->
    <div class="wrapper">
      <header>
        <%= render "shared/navigation" %>
      </header>

      <%= render "shared/flash" %>

      <%= yield %>
    </div>

    <footer class="footer mt-5">
        <span class="mb-2">Feedback or report a problem? <a href="mailto:aurangzaib.danial@gmail.com" class="text-decoration-none text-muted">Email me</a></span>

        <div class="footer-links">
          <%= link_to "Contribute", "https://github.com/aurangzaib-danial/discuss-stirling", target: "_blank"%>
          <%= link_to "Privacy", privacy_path %>
          <%= link_to "Terms", terms_path %>
        </div>
    </footer>
  </body>
</html>
